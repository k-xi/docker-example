{{- if .Values.virtualservice.enabled -}}
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: docker-example-vs
spec:
  hosts:
    - "*"
  gateways:
    - docker-example-gw
  http:
    -  route:
         - destination:
             host: docker-example.com
             port:
               number: 8080
  {{- end }}
